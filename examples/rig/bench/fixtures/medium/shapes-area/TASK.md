# Task: fix the Circle area bug

`pytest -q` fails: `Circle.area()` returns the wrong value. The `shapes` package
splits each shape into its own module (`shapes/rectangle.py`, `shapes/circle.py`).
Find the bug and fix it so the area is correct (π·r²). Do not edit the tests.
